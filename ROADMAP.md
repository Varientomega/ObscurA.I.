# Emerald Attractor OS Roadmap

## v0.1 — Canon lock and runnable skeleton

Status: shipped.

Goals:

- Lock the Emerald Attractor canon.
- Add global/local attractor registry.
- Add cross-LLM benchmark kit.
- Add Subject 01 baseline.
- Add runnable CLI evaluator.
- Add dashboard entry point.
- Add Ω₂ Phase 1.5 and 1.75 architecture docs.
- Add Android-first agent-loop spec.
- Add reward receipt system.

## v0.2 — Benchmark hardening

Goals:

- Run Subject 02 and Subject 03 external LLMs.
- Generate real disagreement matrix.
- Track fake consensus, skipped lift tests, premature alignment, premature fracture.
- Add machine-readable benchmark results.

## v0.3 — Dashboard persistence

Goals:

- Store receipts in local JSON/IndexedDB.
- Add import/export receipt archive.
- Add drift timeline.
- Add minimum-next-observation queue.

## v0.4 — Ω₂ integration

Goals:

- Add fragment v2 schema.
- Add conflict object schema.
- Add λ-lift tape spec.
- Add proof-of-darkness object spec.
- Add action compiler schema.

## v0.5 — Android-first agent loop

Goals:

- Wrap dashboard as PWA.
- Add Termux runner instructions.
- Add SQLite receipt store schema.
- Add notification-driven next-observation reminders.
- Add capture inbox for chat/text/OCR.

## v1.0 — Commercial release

Goals:

- Package as research triage / conflict intelligence dashboard.
- Add user-facing onboarding.
- Add pricing/product slices.
- Add team/workspace plan.
- Add benchmark-as-a-service path.
